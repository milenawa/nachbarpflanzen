<%= form_with(model: impact, local: true) do |form| %>
  <% if impact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(impact.errors.count, "error") %> prohibited this impact from being saved:</h2>

      <ul>
      <% impact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :influencee, "Pflanze", class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= form.collection_select :influencee_id, Plant.all, :id, :name, {prompt: "Pflanze auswählen"}, class: "form-control"%>
    </div>
  </div>
  <div class="form-group row">
    <%= form.label :influencer, "Nachbarpflanze", class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= form.collection_select :influencer_id, Plant.all, :id, :name, {prompt: "Bitte Nachbarpflanze auswählen"}, class: "form-control"%>
    </div>
  </div>
  <div class="form-group row">
    <%= form.label :positive_impact, "gut oder schlecht", class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= form.select :positive_impact, [['Gut', true], ['Schlecht', false]], {}, class: "form-control"%>
    </div>
  </div>
  <div class="form-group row">
    <%= form.label :reason, "Begründung", class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= form.text_field :reason, placeholder: "Begründung", class: "form-control"%>
    </div>
  </div>


  <div class="actions">
    <%= form.submit "Speichern"%>
  </div>
<% end %>
